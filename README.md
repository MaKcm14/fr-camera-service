# How to start the service?
- `git clone https://github.com/MaKcm14/fr-camera-service.git`
- `cd fr-camera-service`
- `python -m venv venv`
- `source venv/bin/activate`
- `pip install -r requirements.txt`
- `python main.py`
